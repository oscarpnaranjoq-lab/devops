name: Hoal Mundo

on: [push]

jobs:

  hola:
    runs-on: ubuntu-latest

    steps:
    - name: Hola Mundo
      run: echo "Hola Mundo Workflow"

  other:
    runs-on: ubuntu-latest

    steps:
    - name: LS
      run: ls -al